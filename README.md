
  # DocGate Medical Booking UI (Community) (Copy) (Copy) (Copy)

  This is a code bundle for DocGate Medical Booking UI (Community) (Copy) (Copy) (Copy). The original project is available at https://www.figma.com/design/8cRNVgjxLUq7p6pVuIZVV9/DocGate-Medical-Booking-UI--Community---Copy---Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  